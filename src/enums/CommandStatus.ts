/**
 * Visual status indicators for commands in the tree view.
 * Maps exit codes to display states.
 */
export enum CommandStatus {
    RUNNING = 'running',
    SUCCESS = 'success',
    FAILED = 'failed'
}