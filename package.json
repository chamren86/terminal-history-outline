{
  "name": "terminal-history-outline",
  "type": "module",
  "displayName": "Terminal History Outline",
  "description": "View and manage terminal command history in the Explorer outline view",
  "version": "0.3.0",
  "engines": {
    "vscode": "^1.93.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "views": {
      "explorer": [
        {
          "id": "terminalHistoryOutline",
          "name": "Terminal History",
          "icon": "$(history)"
        }
      ]
    },
    "commands": [
      {
        "command": "terminalHistory.clear",
        "title": "Clear Terminal History",
        "icon": "$(clear-all)"
      },
      {
        "command": "terminalHistory.rerun",
        "title": "Rerun Command",
        "icon": "$(run)"
      },
      {
        "command": "terminalHistory.copyOutput",
        "title": "Copy Output",
        "icon": "$(copy)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "terminalHistory.clear",
          "when": "view == terminalHistoryOutline",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "terminalHistory.rerun",
          "when": "viewItem == commandItem",
          "group": "inline"
        },
        {
          "command": "terminalHistory.copyOutput",
          "when": "viewItem == commandItem",
          "group": "inline"
        }
      ]
    },
    "configuration": {
      "title": "Terminal History Outline",
      "properties": {
        "terminalHistory.maxHistorySize": {
          "type": "number",
          "default": 100,
          "description": "Maximum number of commands to keep in history"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "npm run compile && mocha --loader=ts-node/esm --ui bdd 'out/test/unit/**/*Test.js'"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.0",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.93.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "strip-ansi": "^7.1.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/chamren86/terminal-history-outline.git"
  },
  "author": "chamren86",
  "license": "MIT",
  "publisher": "chamren86"
}
